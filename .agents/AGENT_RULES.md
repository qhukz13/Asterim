# Agent rules

Superseded on 2026-09-08. The rules live in `AGENTS.md` (repository root) and `CLAUDE.md`; the context lives in `PROJECT_CONTEXT.md`. `blueprint/` is history, not a specification.

Read those three files. Nothing in this directory adds to them.
