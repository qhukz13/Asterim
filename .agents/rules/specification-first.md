# Specification First Rule

The Product Specification (`blueprint`) governs all actions.

If tasked with a feature:

1. Find the feature in the Specification.
2. If it exists, implement it exactly as defined.
3. If it does not exist, reject the task and request a Blueprint update.

Do not write code based on assumptions.
